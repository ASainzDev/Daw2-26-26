<!DOCTYPE html>
<html>
<body>

<?php
	$nombre = array("jd","alex","david","aitor","joel");
    $caracteres;
    foreach($nombre as $name){
    	$caracteres += strlen($name);
    }
    
    echo "<p>El número de caracteres totales en el array es $caracteres <p></br>";
    
    function contarletra($array, $letra){
    	$total;
        
        foreach($array as $name){
        	$total += substr_count($name, $letra);
        }
        
        return $total;
    }
    
    $letra ='a';
    echo contarletra($nombre,$letra);
    
    
?>

</body>
</html>